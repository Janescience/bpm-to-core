# 🔄 Chain Builder - Auto-populate Input Feature

## ภาพรวม

เมื่อใช้ **Chain Builder** เพื่อสร้างลำดับการ transform ข้อมูล ระบบจะ **auto-populate input parameters โดยอัตโนมัติ** สำหรับ steps ที่ 2 เป็นต้นไป

---

## 🎯 ทำไมต้องมี Auto-populate?

### ปัญหาเดิม:
เมื่อสร้าง chain หลาย steps ผู้ใช้ต้อง:
1. ระบุ `jsonField` ใน step แรก
2. ระบุ `jsonField` ใน step ที่ 2, 3, 4... ซ้ำๆ
3. ไม่แน่ใจว่าต้องใส่อะไร เพราะข้อมูลมาจาก step ก่อนหน้าแล้ว

### วิธีแก้:
ระบบจะ **auto-populate input parameters** ให้อัตโนมัติ:
- Step 1: ใช้ JSON data ต้นฉบับ (ต้องระบุ `jsonField`)
- Step 2+: ใช้ผลลัพธ์จาก step ก่อนหน้า (ไม่ต้องระบุ `jsonField`)

---

## 📋 Function Types ที่ Auto-populate

| Function Type | Auto-populate? | คำอธิบาย |
|---------------|----------------|----------|
| **SUBSTRING** | ✅ Yes | ใช้ string จาก step ก่อนหน้า |
| **CONCAT** | ✅ Yes | ใช้ string จาก step ก่อนหน้า concat กับ prefix/suffix |
| **DATE** | ✅ Yes | ใช้ date string จาก step ก่อนหน้า |
| **NUMBER** | ✅ Yes | ใช้ number จาก step ก่อนหน้า |
| **CONFIG** | ✅ Yes | ใช้ค่าจาก step ก่อนหน้าเป็น lookup key |
| **CONDITION** | ✅ Yes | ใช้ค่าจาก step ก่อนหน้าเป็น compare value |
| **CUSTOM** | ✅ Yes | ใช้ผลจาก step ก่อนหน้าเป็น input (auto-populate params ที่เป็น empty string) |
| **DIRECT** | ❌ No | ต้องระบุ jsonField เสมอ |
| **ARRAY_FILTER** | ⚠️ Conditional | Step แรกต้องระบุ jsonField, step ถัดไปไม่ต้อง |

---

## 🎨 UI Indicators

เมื่อ step ที่ 2+ ใช้ function type ที่รองรับ auto-populate จะเห็น **กล่องสีเหลือง**:

```
┌─────────────────────────────────────────────────────┐
│ ℹ️ Input จาก step ก่อนหน้าจะถูกใช้โดยอัตโนมัติ     │
│                                                     │
│ ไม่ต้องระบุ jsonField - จะใช้ผลลัพธ์จาก            │
│ step ก่อนหน้าเป็น input                              │
└─────────────────────────────────────────────────────┘
```

### ข้อความแต่ละ Function Type:

| Function | ข้อความ |
|----------|---------|
| SUBSTRING | "ไม่ต้องระบุ jsonField - จะใช้ผลลัพธ์จาก step ก่อนหน้าเป็น input" |
| CONCAT | "ค่าจาก step ก่อนหน้าจะถูก concat กับ prefix/suffix ที่กำหนด" |
| DATE | "ค่า date จาก step ก่อนหน้าจะถูก format ตามที่กำหนด" |
| NUMBER | "ค่า number จาก step ก่อนหน้าจะถูก format ตามที่กำหนด" |
| CONFIG | "ค่าจาก step ก่อนหน้าจะถูกใช้เป็น lookup key ใน config" |
| CONDITION | "ค่าจาก step ก่อนหน้าจะถูกเปรียบเทียบกับเงื่อนไข" |
| CUSTOM | "ผลลัพธ์จาก step ก่อนหน้าจะถูกส่งเข้า custom function เป็น input" |

---

## 📖 ตัวอย่างการใช้งาน

### ตัวอย่างที่ 1: Filter → Substring

**Scenario:** ดึง HOME address แล้วตัดเหลือ 30 ตัวอักษร

```json
{
  "steps": [
    {
      "type": "ARRAY_FILTER",
      "params": {
        "jsonField": "eAPPDetails.0.Address",  // ← ต้องระบุใน step แรก
        "filterField": "Type",
        "filterValue": "HOME",
        "selectField": "Street"
      }
    },
    {
      "type": "SUBSTRING",
      "params": {
        // ไม่ต้องระบุ jsonField! ระบบใช้ผลจาก step 1 อัตโนมัติ
        "start": 0,
        "length": 30
      }
    }
  ]
}
```

**ข้อมูลที่ไหลผ่าน:**
1. Step 1: `"123 Very Long Street Name That Needs Truncation"`
2. Step 2: `"123 Very Long Street Name Th"` (ตัด 30 ตัวอักษร)

---

### ตัวอย่างที่ 2: Filter → Custom Function → Substring

**Scenario:** ดึง INSURED address (object), สร้าง full address, ตัดเหลือ 30 ตัวอักษร

```json
{
  "steps": [
    {
      "type": "ARRAY_FILTER",
      "params": {
        "jsonField": "eAPPDetails.0.Address",  // ← ต้องระบุใน step แรก
        "filterField": "Category_Addr",
        "filterValue": "INSURED",
        "selectField": ""  // ← empty string = ส่ง object ทั้งก้อน
      }
    },
    {
      "type": "CUSTOM",
      "params": {
        "functionName": "getFullAddress",
        "functionParams": {
          "address": ""  // ← empty string = auto-populate จาก step 1
        }
      }
    },
    {
      "type": "SUBSTRING",
      "params": {
        // ไม่ต้องระบุ jsonField! ระบบใช้ผลจาก getFullAddress
        "start": 0,
        "length": 30
      }
    }
  ]
}
```

**ข้อมูลที่ไหลผ่าน:**
1. Step 1: `{ PlotNumber: "123", BuildingName: "...", MooNumber: "5", ... }` (address object)
2. Step 2: `"123 ม.5 ซอย รามคำแหง ถนน รามคำแหง"` (full address string)
3. Step 3: `"123 ม.5 ซอย รามคำแหง ถนน รา"` (ตัด 30 ตัวอักษร)

**หมายเหตุ:** CUSTOM function จะ auto-populate parameters ที่เป็น empty string (`""`) ด้วยผลลัพธ์จาก step ก่อนหน้า

---

### ตัวอย่างที่ 3: Direct → Config → Condition

**Scenario:** ดึง status code, lookup ใน config, ถ้าไม่มีค่าให้ใส่ "Unknown"

```json
{
  "steps": [
    {
      "type": "DIRECT",
      "params": {
        "jsonField": "eAPPDetails.0.StatusCode"  // ← ต้องระบุใน step แรก
      }
    },
    {
      "type": "CONFIG",
      "params": {
        // ไม่ต้องระบุ jsonField! ระบบใช้ StatusCode เป็น lookup key
        "configKey": "STATUS_CODES",
        "fallbackToSource": false
      }
    },
    {
      "type": "CONDITION",
      "params": {
        // ไม่ต้องระบุ jsonField! ระบบใช้ผลจาก config lookup
        "operator": "isEmpty",
        "trueValue": "Unknown",
        "falseValue": null  // ใช้ค่าจาก step ก่อนหน้า
      }
    }
  ]
}
```

**ข้อมูลที่ไหลผ่าน:**
1. Step 1: `"ACT"` (status code)
2. Step 2: `"Active"` (lookup จาก config)
3. Step 3: `"Active"` (ไม่ว่างเปล่า ใช้ค่าเดิม)

---

## ⚙️ การทำงานภายใน

### Before Auto-populate:
```json
{
  "steps": [
    { "type": "ARRAY_FILTER", "params": { "jsonField": "address", ... } },
    { "type": "SUBSTRING", "params": { "jsonField": "_chainResult", ... } },  // ← ต้องใส่เอง!
    { "type": "CONCAT", "params": { "jsonField": "_chainResult", ... } }      // ← ต้องใส่เอง!
  ]
}
```

### After Auto-populate:
```json
{
  "steps": [
    { "type": "ARRAY_FILTER", "params": { "jsonField": "address", ... } },
    { "type": "SUBSTRING", "params": { "start": 0, "length": 30 } },  // ← ระบบใส่ให้!
    { "type": "CONCAT", "params": { "suffix": "..." } }               // ← ระบบใส่ให้!
  ]
}
```

**Mapping Engine** จะจัดการ `_chainResult` โดยอัตโนมัติ:
```javascript
// lib/mappingEngine.js - transformChain function
for (let i = 0; i < steps.length; i++) {
  let inputData;
  if (i === 0) {
    inputData = jsonData;  // Step แรกใช้ JSON ต้นฉบับ
  } else {
    inputData = { _chainResult: result };  // Step ถัดไปใช้ผล step ก่อนหน้า
    if (!stepParams.jsonField) {
      stepParams.jsonField = '_chainResult';  // ← Auto-populate!
    }

    // Special handling for CUSTOM function: auto-populate empty string params
    if (type?.toUpperCase() === 'CUSTOM' && stepParams.functionParams) {
      for (const [key, value] of Object.entries(stepParams.functionParams)) {
        if (value === '' || value === null || value === undefined) {
          stepParams.functionParams[key] = result;  // ← Auto-populate CUSTOM params!
        }
      }
    }
  }
  result = await transformFunc(inputData, stepParams, context);
}
```

---

## 💡 Best Practices

### ✅ ควรทำ:
1. **ใช้ DIRECT ใน step แรก** ถ้าต้องการดึง field จาก JSON
2. **ใช้ ARRAY_FILTER ใน step แรก** ถ้าต้องการกรอง array
3. **ตรวจสอบ Input Preview** ใน UI ว่า step รับข้อมูลอะไร
4. **ใช้ Output Preview** ใน UI ว่า step ส่งข้อมูลอะไรต่อ

### ❌ ไม่ควรทำ:
1. **ห้ามใส่ jsonField ใน step ที่ 2+** (ระบบใส่ให้อัตโนมัติแล้ว)
2. **ห้ามใช้ DIRECT ใน step ที่ 2+** (ใช้ function อื่นที่ auto-populate ได้)

---

## 🧪 การทดสอบ

### Test Case 1: แสดง Auto-populate Info

1. เปิด Chain Builder
2. เพิ่ม Step 1: ARRAY_FILTER
3. เพิ่ม Step 2: SUBSTRING
4. **ตรวจสอบ:** Step 2 ต้องมีกล่องสีเหลืองแสดง "Input จาก step ก่อนหน้าจะถูกใช้โดยอัตโนมัติ"

### Test Case 2: ไม่แสดง Info ใน Step แรก

1. เปิด Chain Builder
2. เพิ่ม Step 1: SUBSTRING
3. **ตรวจสอบ:** Step 1 ต้องไม่มีกล่องสีเหลือง (เพราะเป็น step แรก)

### Test Case 3: Execute SOAP

1. สร้าง chain: ARRAY_FILTER → SUBSTRING → CONCAT
2. กำหนด parameters เฉพาะ step แรก
3. Execute SOAP
4. **ตรวจสอบ:** ผลลัพธ์ถูกต้องตาม chain

---

## 📚 เอกสารอ้างอิง

- [ARRAY_FILTER_AND_CHAIN_GUIDE.md](ARRAY_FILTER_AND_CHAIN_GUIDE.md) - คู่มือใช้งาน Chain และ Array Filter
- [DYNAMIC_MAPPING_GUIDE.md](DYNAMIC_MAPPING_GUIDE.md) - Function types ทั้งหมด
- [HOW_TO_USE_FUNCTIONS.md](HOW_TO_USE_FUNCTIONS.md) - วิธีใช้งาน functions

---

## 🔍 Troubleshooting

### ปัญหา: Step 2+ ยังต้องใส่ jsonField

**สาเหตุ:** ใช้ function type ที่ไม่รองรับ auto-populate (เช่น DIRECT)

**วิธีแก้:** เปลี่ยนเป็น function type อื่นที่รองรับ auto-populate

### ปัญหา: ไม่เห็นกล่องสีเหลือง

**สาเหตุ:** Step นั้นเป็น step แรก หรือใช้ function type ที่ไม่รองรับ

**วิธีแก้:** ตรวจสอบว่าเป็น step ที่ 2+ และใช้ function type ที่รองรับ

### ปัญหา: ผลลัพธ์ไม่ถูกต้อง

**สาเหตุ:** Parameters ใน step ไม่ถูกต้อง

**วิธีแก้:** ตรวจสอบ Output Preview ของแต่ละ step ว่าส่งข้อมูลอะไรต่อ

---

🎉 **Happy Chaining with Auto-populate!**
